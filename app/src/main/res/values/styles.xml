<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorRed</item>
        <item name="colorPrimaryDark">@color/colorDarkRed</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="exEditText" parent="Theme.Design.Light">
        <item name="colorControlNormal">@color/colorDarkRed</item>
        <item name="colorControlActivated">@color/colorRed</item>
    </style>
</resources>
